const strName = 'Hello Hello javascript world';
const replaceStrWord = strName.replace(/Hello/g, 'Hey');
console.log(replaceStrWord);

//The g flag represents the global search, meaning it will search the entire string for all instances of the word and replace them.
